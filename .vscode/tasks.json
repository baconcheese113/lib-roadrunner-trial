{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build and Run hello_roadrunner",
      "type": "shell",
      "command": "cmake",
      "args": [
        "--build",
        "--preset",
        "default",
        "--target",
        "run"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$msCompile"],
      "detail": "Builds the project and runs the hello_roadrunner executable via CMake's custom 'run' target"
    }
  ]
}
